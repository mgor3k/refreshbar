import XCTest
@testable import RefreshBar

final class RefreshBarTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RefreshBar().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
